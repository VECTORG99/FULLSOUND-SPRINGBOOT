# ============================================================
# FULLSOUND - AWS Production Configuration
# ============================================================

# ==================== SERVER ====================
server.port=8080
server.forward-headers-strategy=framework

# ==================== DATABASE - POSTGRESQL (SUPABASE) ====================
spring.datasource.url=jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.kivpcepyhfpqjfoycwel
spring.datasource.password=AYDoOttgfS3SRw
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================== HIKARI CONNECTION POOL ====================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# ==================== JPA / HIBERNATE ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# ==================== JWT CONFIGURATION ====================
jwt.secret=${JWT_SECRET:MySecretKeyForJWTTokenGenerationShouldBeLongEnoughForHS512AlgorithmFullsoundBackend2025ProductionSecureKey}
jwt.expiration=86400000

# ==================== APPLICATION ====================
spring.application.name=FullSound Backend API
