# ============================================================
# FULLSOUND - Spring Boot Backend Configuration
# ============================================================
# Adaptado para base de datos MySQL existente: Fullsound_Base
# Fecha: 2025-11-13
# ============================================================

# ==================== APPLICATION ====================
spring.application.name=FullSound Backend API
server.port=8080
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html

# ==================== DATABASE - POSTGRESQL ====================
# Configuración para PostgreSQL (Supabase compatible)
# 1. La URL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/postgres}

# 2. El Usuario
spring.datasource.username=${DB_USERNAME:postgres}

# 3. La Contraseña (Tu contraseña de Supabase)
spring.datasource.password=${DB_PASSWORD}

# Driver obligatorio
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
# Deshabilitar prepared statements para Supabase Transaction Pooler (puerto 6543)
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# ==================== JPA/HIBERNATE ====================
# IMPORTANTE: Usar 'validate' en producción, 'update' en desarrollo, 'create' primera vez
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ==================== JWT SECURITY ====================
#  CAMBIAR en producción por un secret de 512+ bits para HS512
jwt.secret=${JWT_SECRET:change-me-in-production-use-a-512-bit-secret}
jwt.expiration=86400000
# 86400000 ms = 24 horas

# ==================== STRIPE PAYMENT ====================
# Configuración deshabilitada (sin pagos reales)
stripe.api.key=sk_test_disabled
stripe.webhook.secret=whsec_disabled

# ==================== FILE UPLOAD ====================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
file.upload.dir=./uploads
file.upload.beats-dir=${file.upload.dir}/beats
file.upload.images-dir=${file.upload.dir}/images

# ==================== ACTUATOR ====================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.export.prometheus.enabled=true

# ==================== LOGGING ====================
logging.level.root=INFO
logging.level.Fullsound.Fullsound=INFO
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Nota: Los niveles DEBUG para desarrollo se activan en el perfil 'dev'
# (ver application-dev.properties)

# ==================== CORS CONFIGURATION ====================
#  AJUSTAR en producción con dominio real
cors.allowed.origins=http://localhost:5173,http://localhost:3000,http://localhost:4200
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*
cors.allow.credentials=true

# ==================== SUPABASE CONFIGURATION ====================
supabase.url=${SUPABASE_URL:https://your-project.supabase.co}
supabase.key=${SUPABASE_KEY:your-supabase-anon-key}
# Para uploads necesitas la service_role key desde variable de entorno
# SUPABASE_SERVICE_KEY=tu_service_role_key_aqui

# ==================== SWAGGER/OPENAPI ====================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
# Deshabilitar Spring Data REST
spring.data.rest.detection-strategy=ANNOTATED
spring.data.rest.base-path=/api/data
springdoc.show-spring-data-rest=false
# Configuración para servidor dinámico
server.forward-headers-strategy=framework

